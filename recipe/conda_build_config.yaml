cxx_compiler:           # [osx]
  - clang_bootstrap     # [osx]

cxx_compiler_version:   # [osx or linux]
  # use an old-enough compiler that allows us
  # to restrict libcxx version
  - 16                  # [osx]
  # linking error with gcc 9
  - 8                   # [linux]

variant:
  - default
  - root_63004  # [linux or osx]
