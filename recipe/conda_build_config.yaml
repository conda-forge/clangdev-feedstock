cxx_compiler:           # [osx]
  - clang_bootstrap     # [osx]

# intentionally keep building for old macOS versions
c_stdlib_version:           # [osx and x86_64]
  - 10.9                    # [osx and x86_64]
MACOSX_DEPLOYMENT_TARGET:   # [osx and x86_64]
  - 10.9                    # [osx and x86_64]

cxx_compiler_version:   # [osx]
  # libcxx 17 requires macOS >=10.13
  - 16                  # [osx]

variant:
  - default
  - root_63008  # [linux or osx]
  - cling_1.0
